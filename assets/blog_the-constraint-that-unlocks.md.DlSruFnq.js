import{_ as a}from"./chunks/the-constraint-that-unlocks.BP42sDgH.js";import{_ as t,o as s,c as i,aa as n}from"./chunks/framework.CuTukyzC.js";import"./chunks/theme.jdWPvDJU.js";const k=JSON.parse('{"title":"The constraint that unlocks","description":"Initialization order is a solved problem in four languages and an unsolved one in the most-used language on earth. One idiom and a 200-line checker close the gap — and pay three more dividends.","frontmatter":{"title":"The constraint that unlocks","description":"Initialization order is a solved problem in four languages and an unsolved one in the most-used language on earth. One idiom and a 200-line checker close the gap — and pay three more dividends.","date":"2026-07"},"headers":[],"relativePath":"blog/the-constraint-that-unlocks.md","filePath":"blog/the-constraint-that-unlocks.md","lastUpdated":1786402255000}'),r={name:"blog/the-constraint-that-unlocks.md"};function o(l,e,h,c,d,p){return s(),i("div",null,[...e[0]||(e[0]=[n('<h1 id="the-constraint-that-unlocks" tabindex="-1">The constraint that unlocks <a class="header-anchor" href="#the-constraint-that-unlocks" aria-label="Permalink to &quot;The constraint that unlocks&quot;">​</a></h1><p><img src="'+a+`" alt="The constraint that unlocks"></p><p>Every JavaScript developer has met the bug. Module A imports B, B imports A, and one of them reads <code>undefined</code> where a class should be — or throws a temporal-dead-zone error, or quietly receives half an <code>exports</code> object. The stack trace points somewhere innocent. The fix is usually to shuffle imports until the symptom moves out of sight.</p><p>Here is the part almost nobody says out loud: <strong>Java has the same disease.</strong> Java happily compiles and runs circular class references — the JVM resolves them lazily — but let two static initializers read each other mid-initialization and you observe <code>null</code> and <code>0</code> where values should be, silently, no error. Different syntax, same failure: <em>reading during initialization</em>. The two most deployed languages in history both hand you this footgun and both call it your fault.</p><h2 id="who-actually-solved-it" tabindex="-1">Who actually solved it <a class="header-anchor" href="#who-actually-solved-it" aria-label="Permalink to &quot;Who actually solved it&quot;">​</a></h2><p>Survey the languages that don&#39;t have this bug, and a pattern falls out. There are exactly two ingredients, and every clean language has at least one:</p><p><strong>Ban cycles at module granularity.</strong> Go refuses package cycles at compile time. Rust refuses crate cycles. OCaml refuses cyclic modules. Java itself — one level up, in JPMS — refuses cyclic <em>modules</em> while tolerating cyclic classes. Go&#39;s version has a beautiful consequence: because the package graph is acyclic, <code>init()</code> order is topologically determined. Acyclicity is what <em>makes</em> initialization order well-defined. The two problems were always one problem.</p><p><strong>Make initialization demand-time.</strong> Swift quietly has the best default in the industry: every global is lazily initialized on first access, by language rule. Rust has no life-before-main at all — statics are compile-time constants or explicitly lazy. Haskell is lazy everywhere for pure values. Erlang resolves module references at <em>call time</em>, so its import graph barely exists as a load-order concept.</p><p>Each language holds one ingredient firmly and pays somewhere else — Haskell solves initialization but cyclic modules need <code>hs-boot</code> ceremony; Go bans cycles but package <code>init()</code> still runs before main. And JavaScript? JavaScript has <strong>neither</strong> ingredient. Cycles are legal, module evaluation is eager, and the traps are always armed.</p><h2 id="the-retrofit" tabindex="-1">The retrofit <a class="header-anchor" href="#the-retrofit" aria-label="Permalink to &quot;The retrofit&quot;">​</a></h2><p><a href="https://github.com/infinite-system/invar" target="_blank" rel="noreferrer">Invar</a> — the terminal editor that <a href="/blog/agents-built-an-editor">AI agents built</a> on <a href="/guide/introduction">ivue</a> — runs on both ingredients, in plain TypeScript, with no language extension. The recipe is two rules.</p><p><strong>Rule one: the module graph is acyclic.</strong> Not as a wish — as a structure. Every file holds one class, so the import graph <em>is</em> the dependency graph, and a cycle has nowhere to hide. Modules layer; lower layers never import upward. This is Go&#39;s compile-time guarantee achieved by grammar.</p><p><strong>Rule two: never read a cross-boundary dependency during construction.</strong> This is the late-read discipline, and it is the whole initialization cure in one sentence. A constructor that reads another module&#39;s state runs at initialization time — whatever order modules happened to load in, that&#39;s the value you cached, forever. A getter that reads the same state runs at <em>demand</em> time, when everything that exists, exists:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light one-dark-pro vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#E5C07B;"> $Workspace</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-light-font-style:inherit;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">  // WRONG — initialization-time read. Whatever \`editorSource\` held at</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-light-font-style:inherit;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">  // construction is frozen into this field for the object&#39;s lifetime.</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#E06C75;">  activeEditor</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#E5C07B;"> this</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;">ports</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;">editorSource</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;">active</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-light-font-style:inherit;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">  // RIGHT — demand-time read. Resolves when asked, never before.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">  protected</span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;"> get</span><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;"> activeEditor</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#E5C07B;"> this</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;">ports</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;">editorSource</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;">active</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>This is not theoretical hygiene. Mid-campaign, one converted class cached a wrap-width ref in its constructor — initialization-time read — and word-wrap silently broke for every document opened after the first. The referee&#39;s byte-level gate caught it; the fix was moving one read from a field to a getter. The disease and the cure, four lines apart.</p><p>Swift makes globals lazy by fiat. Rust makes you write <code>OnceLock</code>. The late-read getter is JavaScript&#39;s version, and with <a href="/blog/reactive-is-all-you-need">Reactive classes</a> it is not even a sacrifice — a getter over reactive state is <em>live</em>, so the demand-time read is also always the current read.</p><h2 id="the-unlock" tabindex="-1">The unlock <a class="header-anchor" href="#the-unlock" aria-label="Permalink to &quot;The unlock&quot;">​</a></h2><p>Here is why this post is not called &quot;a workaround for circular imports.&quot; Swift&#39;s lazy globals solve initialization and give you nothing else. The late-read getter solves initialization and then keeps paying:</p><p><strong>It is the test seam.</strong> A protected getter is an override point. Want to count how often a document measures line widths? Subclass it, override one getter, count. No mocking framework, no injection container — the discipline that fixes initialization <em>is</em> the dependency injection.</p><p><strong>It is the hot-swap point.</strong> Every class in the system is published through a mutable binding — <code>export let Class</code> — and consumed through late reads. Nothing cached a constructor at initialization time, so <a href="/blog/hot-reload-for-logic">swapping an implementation at runtime</a> is safe by construction, not by luck.</p><p><strong>It keeps the graph reshapeable.</strong> When no object froze another object&#39;s address at birth, you can cut any subtree of the <a href="/blog/the-object-graph-they-took">object graph</a> and re-hang it elsewhere, and every reference resolves correctly at the next read. Invar&#39;s entire pane system was restructured twice in one week — thousands of lines each time — at the cost of the diff, because nothing anywhere held a stale reference by design.</p><p>One constraint, four payoffs. That asymmetry is the signature of a real invariant rather than a coding tip: it doesn&#39;t just prevent the failure class it was aimed at, it generates capabilities nobody designed into it.</p><h2 id="the-referee-makes-it-law" tabindex="-1">The referee makes it law <a class="header-anchor" href="#the-referee-makes-it-law" aria-label="Permalink to &quot;The referee makes it law&quot;">​</a></h2><p>None of this survives as culture. Conventions decay under deadline pressure and new contributors — and AI agents writing most of the code makes &quot;just remember not to&quot; a fantasy. So the rules are enforced the way Go enforces its package rule: mechanically, before anything lands.</p><p>A ~200-line AST checker parses every file and rejects, at the merge gate, module-level state that should live on a class, detached functions with no extension seam, constructor shapes that invite early reads, and files whose import structure hides the dependency graph. Sixteen structural rules, every module enforced, every commit. The checker took an afternoon. It is the difference between &quot;our team prefers late reads&quot; and &quot;an initialization-order bug cannot merge.&quot;</p><h2 id="the-claim-sized-honestly" tabindex="-1">The claim, sized honestly <a class="header-anchor" href="#the-claim-sized-honestly" aria-label="Permalink to &quot;The claim, sized honestly&quot;">​</a></h2><p>Nothing here required inventing new computer science — Go, Rust, Swift, and Erlang each proved one ingredient at language scale. The contribution is narrower and, we think, more useful: <strong>the most-used language on earth can have Swift-grade initialization safety and Go-grade graph discipline today, retrofitted by one idiom and one small checker, with no committee and no compiler fork.</strong> And unlike the native versions, this one hands back a test seam, a hot-swap point, and a permanently reshapeable object graph as change.</p><p>The constraint costs a getter where you wanted a field. It unlocks everything else.</p>`,28)])])}const y=t(r,[["render",o]]);export{k as __pageData,y as default};
