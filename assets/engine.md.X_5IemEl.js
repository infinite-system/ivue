import{_ as s,o as i,c as a,aa as t}from"./chunks/framework.CAKUfmHW.js";import"./chunks/theme.daE0n-Ug.js";const g=JSON.parse(`{"title":"The Engine","description":"Vue's reactivity engine can track a derived value without allocating a separate derivation node — computed() is a cache annotation, not the derivation primitive. The class shape exposes what was always there.","frontmatter":{"title":"The Engine","description":"Vue's reactivity engine can track a derived value without allocating a separate derivation node — computed() is a cache annotation, not the derivation primitive. The class shape exposes what was always there."},"headers":[],"relativePath":"engine.md","filePath":"engine.md","lastUpdated":1785304253000}`),n={name:"engine.md"};function r(o,e,h,l,p,d){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="the-engine" tabindex="-1">The Engine <a class="header-anchor" href="#the-engine" aria-label="Permalink to &quot;The Engine&quot;">​</a></h1><p>ivue&#39;s central discovery is not a technique. It is a claim about Vue itself:</p><blockquote><p><strong>Vue&#39;s reactivity engine has a native, zero-node mode for derived values — and the standard idiom hides it.</strong> <code>computed()</code> was never the derivation primitive. It is a <em>cache annotation</em>. The derivation primitive is the tracked read, and it flows through plain functions without creating another reactive node.</p></blockquote><p>ivue doesn&#39;t patch Vue, wrap Vue, or compete with Vue. It changes the <em>authoring geometry</em> so the engine&#39;s cheapest mode becomes the default instead of the exception. Same engine, different expression — and the difference is measured in orders of magnitude.</p><h2 id="what-the-engine-actually-does" tabindex="-1">What the engine actually does <a class="header-anchor" href="#what-the-engine-actually-does" aria-label="Permalink to &quot;What the engine actually does&quot;">​</a></h2><p>Strip every idiom away and Vue&#39;s reactivity is three moves:</p><ol><li>reads of reactive leaves (<code>ref.value</code>, reactive props) are <strong>tracked</strong> against whatever effect is currently running;</li><li>writes <strong>notify</strong> the effects that read them;</li><li>tracking flows <strong>through arbitrary function calls</strong> — the engine neither knows nor cares how many stack frames sit between the effect and the leaf.</li></ol><p>That third move is the buried one. When a render effect reads a plain getter, the getter&#39;s body executes <em>inside the effect</em> — so every leaf it touches subscribes the effect directly. The getter is a transparent corridor:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light one-dark-pro vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#E5C07B;"> $Cart</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">  get</span><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;"> items</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">    return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;"> ref</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">&lt;{ </span><span style="--shiki-light:#E36209;--shiki-dark:#E06C75;">price</span><span style="--shiki-light:#D73A49;--shiki-dark:#ABB2BF;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#E5C07B;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;"> }[]&gt;([]);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">  }</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-light-font-style:inherit;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">  // No computed(). No graph node. Fully reactive.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">  get</span><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;"> total</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#E5C07B;"> this</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;">items</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;">value</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;">reduce</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">((</span><span style="--shiki-light:#E36209;--shiki-light-font-style:inherit;--shiki-dark:#E06C75;--shiki-dark-font-style:italic;">sum</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#E36209;--shiki-light-font-style:inherit;--shiki-dark:#E06C75;--shiki-dark-font-style:italic;">item</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;"> sum</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> +</span><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;"> item</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;">price</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#D19A66;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>A template reading <code>cart.total</code> re-renders when <code>items</code> changes — with <strong>zero per-instance reactive machinery</strong> for <code>total</code> itself. No <code>ComputedRefImpl</code>, no dependency links, no invalidation bookkeeping, no per-instance bytes. The engine has supported this since Vue 3.0 — it is a direct consequence of how tracking works, not a feature anyone had to add.</p><p>So what does <code>computed()</code> actually buy? Exactly one thing: <strong>memoization</strong> — with ~300 bytes per instance, a graph node, and invalidation bookkeeping as the price (<a href="/guide/performance#memory-derivations-weigh-nothing">the honest numbers</a>). Caching is a real service that is <em>sometimes</em> worth buying. It was never the way derivation works.</p><h2 id="why-the-idiom-buried-it" tabindex="-1">Why the idiom buried it <a class="header-anchor" href="#why-the-idiom-buried-it" aria-label="Permalink to &quot;Why the idiom buried it&quot;">​</a></h2><p>The official idiom — docs, tutorials, the entire ecosystem — teaches &quot;derived state = computed property.&quot; If the engine doesn&#39;t need it, why does the idiom insist on it?</p><p>Because of <strong>closure geometry</strong> — and this is the honest part: Vue&#39;s idiom is <em>correct for Vue&#39;s authoring shape</em>. Inside <code>setup()</code>, there is no good home for a live derived value:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light one-dark-pro vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;">setup</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#E5C07B;"> items</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;"> ref</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">([]);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#E5C07B;"> total</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;"> items</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;">value</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;">reduce</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#ABB2BF;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">);   </span><span style="--shiki-light:#6A737D;--shiki-light-font-style:inherit;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// ✗ computes ONCE — stale forever</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">  const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;"> total</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;"> items</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;">value</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;">reduce</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#ABB2BF;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">); </span><span style="--shiki-light:#6A737D;--shiki-light-font-style:inherit;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// ✗ works, but {{ total() }} everywhere</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#E5C07B;"> total</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;"> computed</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">=&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#ABB2BF;"> ...</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">);       </span><span style="--shiki-light:#6A737D;--shiki-light-font-style:inherit;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// ✓ the only ergonomic option</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>A <code>const</code> evaluates once and dies. A function works but breaks template ergonomics. <code>computed()</code> is the only form that is both live and reads like a value — <strong>so the idiom reached for it universally, and the cache tax came along silently</strong>. Multiply by every derived value, every component, every instance of every component, and the ecosystem&#39;s default became: pay for memoization everywhere, need it almost nowhere.</p><p>The idiom wasn&#39;t wrong. It was the best expression available <em>in closure geometry</em>. The engine&#39;s cheapest mode simply has no syntax there.</p><h2 id="what-the-class-shape-excavates" tabindex="-1">What the class shape excavates <a class="header-anchor" href="#what-the-class-shape-excavates" aria-label="Permalink to &quot;What the class shape excavates&quot;">​</a></h2><p>A class has the one thing a closure lacks: a <strong>prototype</strong> — a place where a derived value can live <em>once</em>, stay <em>live</em> (getters re-execute per read), and read like a value (<code>cart.total</code>, no parentheses). The three requirements that forced <code>computed()</code> in closures are all met without per-instance derivation machinery:</p><table tabindex="0"><thead><tr><th>requirement</th><th>closure</th><th>class</th></tr></thead><tbody><tr><td>stays live</td><td>only <code>computed()</code>/fn</td><td>getter re-runs per read</td></tr><tr><td>reads like a value</td><td>only <code>computed()</code></td><td>getter — <code>cart.total</code></td></tr><tr><td>costs nothing per instance</td><td>✗ (closure or cell each)</td><td>prototype — shared, 0 bytes</td></tr></tbody></table><p>That&#39;s the entire excavation. The class shape doesn&#39;t add a capability to Vue — it gives Vue&#39;s <em>existing</em> cheapest mode an ergonomic syntax, at which point <code>computed()</code> collapses back to what it always was: a surgical opt-in for the rare derivation where caching pays (<a href="/guide/computed-watch#computed-your-usememo">when, exactly</a>).</p><p>The transformation happens once per prototype; steady execution remains stable, ordinary JavaScript property access. <a href="/guide/performance#designed-for-native-jit-shapes">Performance by Design</a> explains how that structure gives modern just-in-time compilers consistent shapes to specialize without making runtime optimization part of ivue&#39;s correctness.</p><h2 id="what-falls-out" tabindex="-1">What falls out <a class="header-anchor" href="#what-falls-out" aria-label="Permalink to &quot;What falls out&quot;">​</a></h2><p>Consequences observed in production, not projected:</p><ul><li><strong>A census, not a vibe.</strong> A shipped reader application — virtualized scrolling of 99,925-paragraph documents, seek, search, autoplay, inline editing — runs on <strong>three</strong> <code>computed()</code>s across ~3,900 lines: one expensive search sweep, one render-suppressing window snapshot, one stable watched handle. Every other derived value in ~170 getters is a plain getter. Each of the three maps to one of the doctrine&#39;s named exceptions; a fourth was deleted when it failed the test.</li><li><strong><code>computed()</code> becomes signal.</strong> When the keyword appears three times instead of three hundred, each occurrence <em>means something</em>: &quot;the derivation behind this is expensive.&quot; The annotation&#39;s rarity is documentation.</li><li><strong>Instance cost collapses.</strong> Derivations weigh nothing per instance, so 10k-row virtualized lists stop paying megabytes of bookkeeping — <a href="/guide/performance">6–132× faster creation, up to 5× less live heap</a>.</li><li><strong>The graph stays constant-size.</strong> Reactive-graph size stops scaling with feature count or data size; it scales with <em>how many caches you deliberately bought</em>. Complexity becomes locally auditable — each new getter&#39;s cost is readable off its body.</li><li><strong>Development matches production.</strong> The engine uses native construction and direct method binding in every environment. Vite and Vue rebuild the owner after script edits, so no development proxy or dispatch layer changes the class geometry being tested (<a href="/guide/hmr">Development &amp; HMR</a>).</li></ul><h2 id="convergence-—-others-keep-finding-the-same-invariant" tabindex="-1">Convergence — others keep finding the same invariant <a class="header-anchor" href="#convergence-—-others-keep-finding-the-same-invariant" aria-label="Permalink to &quot;Convergence — others keep finding the same invariant&quot;">​</a></h2><p>A real invariant gets rediscovered from independent directions:</p><ul><li><strong>Solid.js</strong> documents it outright: derived values are plain functions; <code>createMemo</code> only when memoization pays. Same invariant, closure-geometry dialect (<a href="/guide/model-layer#what-about-solid-js">the full comparison</a>).</li><li><strong>Vue 3.4</strong> shipped equality-based propagation stops for computeds — the team optimizing the cache tax the idiom institutionalized.</li><li><strong>MobX</strong> made <code>computed</code> opt-in decoration over plain class getters a decade ago — different engine, same geometry conclusion.</li></ul><p>ivue&#39;s contribution is not the invariant — it is expressing the invariant <em>inside Vue&#39;s own engine</em>, unmodified, so a Vue codebase gets it without changing frameworks: zero patches, standard <code>ref()</code>/<code>computed()</code>/<code>watch</code> underneath, 1.1 kB of glue.</p><h2 id="the-claim-precisely" tabindex="-1">The claim, precisely <a class="header-anchor" href="#the-claim-precisely" aria-label="Permalink to &quot;The claim, precisely&quot;">​</a></h2><p><strong>Claimed:</strong> Vue&#39;s engine natively supports zero-cost, fully-reactive derivation through plain getters; <code>computed()</code> is semantically a cache annotation; the closure authoring shape is what made it look like a derivation primitive; a class shape restores the engine&#39;s default, and the practical need for <code>computed()</code> drops to the low single digits per thousands of lines.</p><p><strong>Not claimed:</strong> that the Vue team doesn&#39;t understand their own engine (they built the mechanism this page describes); that <code>computed()</code> is bad (three of them are load-bearing in the flagship case study); that this holds outside Vue&#39;s fine-grained tracking model (it doesn&#39;t — React has no tracked reads to flow through).</p><p><strong>Falsifiable:</strong> census your own ivue codebase. If honest application of the doctrine — cache only for expensive work, render suppression, or a stable watched handle — leaves you needing <code>computed()</code> for a substantial fraction of your derived values, this page is wrong. Measured so far: 3 in 3,902 lines, under production load, at 99,925 items.</p>`,33)])])}const u=s(n,[["render",r]]);export{g as __pageData,u as default};
