import{_ as t}from"./chunks/agents-built-an-editor.BQd2eNyQ.js";import{_ as a,o as i,c as s,aa as n}from"./chunks/framework.CAKUfmHW.js";import"./chunks/theme.daE0n-Ug.js";const b=JSON.parse('{"title":"AI agents built a 26,000-line code editor on ivue","description":"A working terminal code editor, written almost entirely by AI agents — zero cycle-breaking hacks, one computed() in the whole codebase, one wiring seam. What a substrate is worth when the author is a machine.","frontmatter":{"title":"AI agents built a 26,000-line code editor on ivue","description":"A working terminal code editor, written almost entirely by AI agents — zero cycle-breaking hacks, one computed() in the whole codebase, one wiring seam. What a substrate is worth when the author is a machine.","date":"2026-07"},"headers":[],"relativePath":"blog/agents-built-an-editor.md","filePath":"blog/agents-built-an-editor.md","lastUpdated":1785304253000}'),o={name:"blog/agents-built-an-editor.md"};function r(h,e,l,c,d,p){return i(),s("div",null,[...e[0]||(e[0]=[n('<h1 id="ai-agents-built-a-26-000-line-code-editor-on-ivue" tabindex="-1">AI agents built a 26,000-line code editor on ivue <a class="header-anchor" href="#ai-agents-built-a-26-000-line-code-editor-on-ivue" aria-label="Permalink to &quot;AI agents built a 26,000-line code editor on ivue&quot;">​</a></h1><p><img src="'+t+`" alt="AI agents built a 26,000-line code editor on ivue"></p><p>There is a terminal code editor called <a href="https://github.com/infinite-system/invar" target="_blank" rel="noreferrer">Invar</a> — named after the iron–nickel alloy engineered in 1896 to have near-zero thermal expansion; precision clocks were built from it because <strong>it does not drift</strong>. The editor has a file tree, a real text editor with word wrap and emoji-aware columns, fuzzy go-to-file, find &amp; replace, a git panel with side-by-side diffs and staging, LSP hover cards, a command palette, and tabs. It runs on Bun, renders through a terminal framebuffer, and its entire model layer is ivue.</p><p>It was built almost entirely by AI agents, in 292 commits. And the measurements it produces are the point of this post.</p><p>Counted over the repository (all <code>src/**/*.ts</code>, July 2026): 153 TypeScript files, 25,890 lines including its 57 test files. 42 classes export through <code>Reactive()</code>. 34 stateless capability classes export through <code>Static()</code>. And two counts that would run to dozens in most codebases this size:</p><ul><li>Lazy imports or comment-marked workarounds that exist to break circular dependencies: <strong>zero</strong>.</li><li>Derivation caches — <code>computed()</code> calls: <strong>one</strong>, in the entire editor.</li></ul><p>Those numbers are not tidiness. They are the substrate doing work.</p><h2 id="no-dom-anywhere" tabindex="-1">No DOM anywhere <a class="header-anchor" href="#no-dom-anywhere" aria-label="Permalink to &quot;No DOM anywhere&quot;">​</a></h2><p>First, the quiet result: there is no Vue component, no SFC, and no DOM in this program. ivue drives a terminal — classes hold state in ref-getters, watchers repaint the cells that changed, and Vue&#39;s reactivity runs as pure signal machinery under a framebuffer.</p><p>ivue was never a component accessory. It is a model layer that happens to plug into Vue templates when templates exist. An editor that ships without a single component is the strongest form of that claim.</p><h2 id="the-author-changed-so-the-substrate-math-changed" tabindex="-1">The author changed, so the substrate math changed <a class="header-anchor" href="#the-author-changed-so-the-substrate-math-changed" aria-label="Permalink to &quot;The author changed, so the substrate math changed&quot;">​</a></h2><p>Almost nobody hand-writes 26,000 lines anymore. The economics of software authorship have shifted to agents — and agents change what a substrate is worth, because agents are <strong>stochastic rule-followers</strong>. Ask one to follow a convention and it complies at some rate slightly below 100%. Per edit, the gap is negligible. Over hundreds of commits, a per-edit violation rate compounds into a certainty.</p><p>That splits every safety property into two kinds:</p><blockquote><p>Discipline-based safety degrades with scale. Construction-based safety doesn&#39;t.</p></blockquote><p>A rule that says <em>be careful about import cycles</em> is discipline: it fails stochastically, and a large codebase takes enough edits to make the failure guaranteed. A pattern where the dangerous moment structurally cannot occur is construction: its violation rate is zero no matter how many edits land, because there is nothing to violate.</p><p>The wider ecosystem shows what discipline costs at scale. Interactive apps built on coarse-grained re-rendering evolve the same organs under pressure, in public: hand-rolled external stores with selector subscriptions to escape subtree re-renders, hundreds of memoization call sites, and finally a compiler whose job is to automate the memoization. Automating a tax is real progress — but it is not the same as the tax not existing. Fine-grained reactivity — the model Solid.js runs on, the model Svelte 5&#39;s runes adopted, the model Vue itself is moving toward with Vapor — has no such tax to automate. In ivue, a ref change re-runs exactly the effects that read it, and a derivation is a plain getter — re-run on tracked reads, costing zero bytes per instance.</p><p>That is why the editor contains exactly one <code>computed()</code> in 26,000 lines. Its gutter diff map caches behind <code>computed()</code> because alignment is document-sized work while cursor repaints are frequent — and the call site carries a comment saying precisely that. One cache, opted into surgically, with a written justification; every other derivation in the editor is a plain getter. That is <a href="/blog/computed-is-a-cache">the doctrine</a> — <code>computed()</code> is a cache, not a derivation — surviving contact with a real editor, at a ratio of one to everything else.</p><h2 id="cycles-can-t-happen-so-no-one-fights-them" tabindex="-1">Cycles can&#39;t happen, so no one fights them <a class="header-anchor" href="#cycles-can-t-happen-so-no-one-fights-them" aria-label="Permalink to &quot;Cycles can&#39;t happen, so no one fights them&quot;">​</a></h2><p>The deeper zero is the circular one. Module cycles are the classic scale disease: <code>Cannot access &#39;X&#39; before initialization</code>, files created solely to host a constant that two modules both need, lazy imports buried inside functions with a comment explaining which cycle they break. Every one of those artifacts is discipline — a patch applied at one edge of the import graph, by someone who got bitten there.</p><p>The <a href="/guide/modules">namespace pattern</a> removes the disease instead of treating bites. Every cross-module reference is late-bound — getter bodies, method bodies, a <code>Static()</code> seam — so nothing reads another module&#39;s value while modules are still initializing. The import graph can contain cycles that never manifest, because binding happens at call time, not load time.</p><p>The property is <strong>per-edge</strong>, and that makes it scale-invariant: it holds identically at 5,000 lines and 500,000, because each reference is still late-bound regardless of how many exist. The editor&#39;s git, workspace, editor, and UI modules reference each other freely across 153 files. Chains like <em>workspace → active tab → editor → viewport</em> compose through the graph without a single ordering concern — and the grep for cycle workarounds returns zero, after 292 commits by authors who were never once told to be careful.</p><p>For an agent fleet, this is the whole ballgame. An agent that hits a cycle failure burns a session diagnosing initialization order — and every workaround it lands becomes <strong>context tax on every future session</strong>, which must rediscover the hack, understand why it exists, and preserve it. An invariant that holds by construction costs zero context, forever. The cheapest thing an agent can maintain is a codebase where the failure class isn&#39;t in its world.</p><h2 id="one-seam-applied-34-times" tabindex="-1">One seam, applied 34 times <a class="header-anchor" href="#one-seam-applied-34-times" aria-label="Permalink to &quot;One seam, applied 34 times&quot;">​</a></h2><p>The editor&#39;s stateless capabilities — file system access, git commands, parsers, clocks, clipboard, status channels — all export through the same three-line shape:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light one-dark-pro vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;"> namespace</span><span style="--shiki-light:#6F42C1;--shiki-dark:#E5C07B;"> Files</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">  export</span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#E5C07B;"> $Class</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;"> $Files</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">; </span><span style="--shiki-light:#6A737D;--shiki-light-font-style:inherit;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// raw — children \`extends\` this</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">  export</span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;"> let</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;"> Class</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;"> Static</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;">$Class</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">); </span><span style="--shiki-light:#6A737D;--shiki-light-font-style:inherit;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// bound — you call this</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p><a href="/guide/static"><code>Static()</code></a> binds the class&#39;s methods lazily with stable identity, and the mutable <code>Class</code> slot stays replaceable — by a test double, a plugin, a fork — with no dependency-object threading. A human team might debate whether one uniform seam beats a bespoke, explicitly-typed injection shape per subsystem. For an agent the question dissolves: applying one known pattern is the thing it does at the highest fidelity, and 34 identical seams cost less to write, verify, and gate than 34 designed ones. The project enforces the seam with a static-analysis check in its merge gate, so the uniformity is construction too, not habit.</p><h2 id="gates-instead-of-vigilance" tabindex="-1">Gates instead of vigilance <a class="header-anchor" href="#gates-instead-of-vigilance" aria-label="Permalink to &quot;Gates instead of vigilance&quot;">​</a></h2><p>The same philosophy runs through how the editor verifies itself. Each module carries a colocated invariants contract — the load-bearing rules of its domain, each stating what becomes impossible if the rule holds. A merge gate verifies them by driving the real user path: injecting input, reading the rendered framebuffer, and blocking any commit that regresses a contract. Not &quot;the agents were careful&quot; — the gate makes carelessness unmergeable.</p><p>That is one lesson generalized: never trust discipline where you can install construction. Choose substrates where the failure classes are absent, write contracts for what remains, and let gates — not vigilance — hold the line. Agents thrive under exactly this regime, because it converts quality from a per-edit probability into a structural fact.</p><h2 id="where-the-gate-came-from" tabindex="-1">Where the gate came from <a class="header-anchor" href="#where-the-gate-came-from" aria-label="Permalink to &quot;Where the gate came from&quot;">​</a></h2><p>The part no grep can show: the discipline was not installed on day one. The editor is built semi-autonomously — agents write nearly all of the code, and one person steers. The steering has a single, repeated shape: at every decision, push the work toward its <em>invariants</em> — the rules of the domain that hold regardless of implementation, each stated with what becomes impossible if it holds. Out of that repetition the project&#39;s structure crystallized: the colocated contracts, then the merge gate that verifies them by driving the real user path, then the static checks that enforce the seam. The agents built their own quality infrastructure, because the guidance kept demanding a standard the infrastructure is the cheapest way to meet.</p><p>That is the general recipe this project demonstrates, and none of it is editor-specific. Pick a substrate whose failure classes are absent by construction. State the domain&#39;s invariants explicitly. Let agents generate against them, and let gates — not vigilance — hold the line. The human contribution concentrates where it is irreplaceable: knowing <em>which</em> rules are load-bearing. The result here is 25,890 lines with one cache, zero cycle workarounds, and zero apologies — an editor that holds its shape under heat, like the alloy it is named for.</p><p>The model layer under all of it is <a href="/blog/one-kilobyte-feature">1.1 kB</a>.</p><hr><p><em>The numbers in this post are measured, not promised: file, line, commit, and call-site counts come from <code>find</code>, <code>wc</code>, <code>git log</code>, and <code>grep</code> over <a href="https://github.com/infinite-system/invar" target="_blank" rel="noreferrer">the editor&#39;s repository</a> in July 2026 — run them yourself. The reactivity and teardown claims about ivue itself are the engine&#39;s <a href="/reference/invariants">documented invariants</a>, verified by its test suite at 100% coverage.</em></p>`,35)])])}const f=a(o,[["render",r]]);export{b as __pageData,f as default};
