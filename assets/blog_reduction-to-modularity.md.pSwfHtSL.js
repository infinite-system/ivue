import{_ as t}from"./chunks/reduction-to-modularity.CVBQKd1b.js";import{_ as i,o as a,c as o,aa as n}from"./chunks/framework.CAKUfmHW.js";import"./chunks/theme.daE0n-Ug.js";const f=JSON.parse(`{"title":"Reduction to modularity, vibe-coded","description":"In one day, AI agents extracted a monolith's git, markdown, file tree, settings, and keybindings into plugins behind enforced boundaries — directed by one human talking casually. The care didn't come from the model. It was moved into the structure.","frontmatter":{"title":"Reduction to modularity, vibe-coded","description":"In one day, AI agents extracted a monolith's git, markdown, file tree, settings, and keybindings into plugins behind enforced boundaries — directed by one human talking casually. The care didn't come from the model. It was moved into the structure.","date":"2026-07"},"headers":[],"relativePath":"blog/reduction-to-modularity.md","filePath":"blog/reduction-to-modularity.md","lastUpdated":1785304253000}`),r={name:"blog/reduction-to-modularity.md"};function s(d,e,c,h,l,u){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="reduction-to-modularity-vibe-coded" tabindex="-1">Reduction to modularity, vibe-coded <a class="header-anchor" href="#reduction-to-modularity-vibe-coded" aria-label="Permalink to &quot;Reduction to modularity, vibe-coded&quot;">​</a></h1><p><img src="'+t+'" alt="Reduction to modularity, vibe-coded"></p><p>Modularizing a working application is the kind of refactor that is usually done meticulously, by senior humans, over a quarter: draw the boundaries, argue about them in design review, migrate one subsystem at a time, hold the line in code review forever after.</p><p>On July 26, 2026, the <a href="https://github.com/infinite-system/invar" target="_blank" rel="noreferrer">Invar</a> editor went through that entire arc in one day — 76 commits on main, every one behind a mechanical merge gate — and the human directing it was, in his own words, <em>vibe coding it</em>. Messages like &quot;the glyph for git is kinda sucky though&quot; and &quot;browsing commits is not fun and doesnt work properly&quot; went in; gated, invariant-recorded architecture came out.</p><p>What landed, in order:</p><ul><li><strong>Workspace became a pure canvas.</strong> Git state, diff state, and markdown left the host. 43 scattered mode checks collapsed into 2 capability questions. <code>grep -icE &quot;diff|markdown&quot;</code> over the host returns 0, and a boundary check in the gate fails any commit where host code names a plugin.</li><li><strong>The plugin kinds got named</strong> — and the taxonomy was tested against every existing citizen before it was trusted. <em>Contributors</em> push registrations into the canvas. <em>Providers</em> answer typed questions. <em>Hosted runtimes</em> exchange an owned stream with one reactive owner. One authority per boundary contract, with recorded impossibilities: a provider cannot paint; a contributor cannot answer position queries; a runtime cannot touch the reactive graph directly.</li><li><strong>The file tree became a plugin</strong> — after the reduction forced an honest split: document opening is host furniture (Quick Open, tabs, and navigation never consulted the tree), the tree view is a contributor. A zero-plugin build has no sidebar and that is now a literal, recorded claim rather than a slogan.</li><li><strong>Plugins got their own settings and keybindings.</strong> Not a manifest file format — typed registrations on the existing contribution context. Plugin bindings form a registry layer above the host floor and below user rebinds; a plugin that tries to register a <em>reserved</em> chord is refused at registration. The acceptance test overshot: the count of source-control lines in the host keybinding table went from 13 to <strong>0</strong>.</li><li><strong>Keyboard ownership got its invariant</strong>: the focused surface owns the keystroke; the host claims a minimal reserved set, each chord carrying a written warrant. Every F-key was retired for modifier chords — and every replacement was <em>proven to arrive</em> through a real PTY on both terminal parser generations, because measuring the codebase&#39;s own claims found three of them false, including the one the whole F-key design had rested on.</li></ul><p>The same day also produced structural code folding (folding and word wrap now contribute to one shared line-to-row generator, because two mappings consulted by different consumers <em>will</em> disagree), inline AI rewrites through a swappable provider port, two operating-system-level root causes in the PTY layer, and a machine-wide scheduling lock that turned a fog of &quot;flaky tests&quot; into a two-way classifier: load flakes versus real intermittent defects with nowhere left to hide.</p><h2 id="the-trick-is-that-there-is-no-trick" tabindex="-1">The trick is that there is no trick <a class="header-anchor" href="#the-trick-is-that-there-is-no-trick" aria-label="Permalink to &quot;The trick is that there is no trick&quot;">​</a></h2><p>Nothing about the model changed at midnight. What made casual direction produce architecture-grade output is that <strong>the meticulousness humans usually supply by vigilance had been moved into the substrate</strong>, in three layers.</p><p><strong>The invariant contracts are the design review.</strong> The repository carries 32 recorded invariants, each with a scope, a mechanism, and — the load-bearing part — an <em>impossible-if-true</em> list. &quot;A claim may not derive its occupancy from the aggregate it feeds&quot; exists because an extraction produced a boot-time cycle once; now every later extraction is checked against it mechanically. A human architect applies such rules by remembering them in review. Here, forgetting is not an available failure mode.</p><p><strong>The gate converts vibes into evidence.</strong> Every landing runs the type checker, 1,500+ unit tests, ~60 driven PTY smokes that operate the real program through a real terminal, a plugin-boundary scan, a coverage ratchet that refuses undeclared assertion loss, and a performance trend detector with a positive control. The human can afford to say &quot;something is nagging the scrolling&quot; precisely because the system demands the fix arrive with paired measurements and a ratchet. Loose input is affordable when output discipline is mechanical.</p><p><strong>ivue removes the failure class that punishes speed hardest.</strong> The classic modularization killer is the mysterious initialization-order bug — works unless module A loads before module B. The <code>Static()</code> / <code>Reactive()</code> namespace discipline resolves members at call time, not import time, so there is no hidden load order to get wrong; attach order is explicit data in the plugin contracts. The two dependency cycles the extractions did produce failed <em>instantly and loudly</em> — a stack overflow at boot, caught by a smoke, fix site in the trace. The bugs that remain are the debuggable kind. Velocity stops compounding into swamp.</p><h2 id="the-part-the-human-still-does" tabindex="-1">The part the human still does <a class="header-anchor" href="#the-part-the-human-still-does" aria-label="Permalink to &quot;The part the human still does&quot;">​</a></h2><p>Reading that day&#39;s transcript, the human&#39;s contribution is not carefulness — it is <em>judgment at the reduction points</em>. Deciding that document-opening is host furniture but the tree view is not. Deciding the capsule vision waits until the architecture is refined, but its simple core (per-workspace agents that resume their sessions) folds into the extraction. Vetoing a glyph. Noticing that browsing commits &quot;is not fun.&quot;</p><p>Everything below that judgment line — the boundary enforcement, the proof that a chord actually arrives, the check that a hover doesn&#39;t paint one row too low, the discipline of never landing on a red without proving the red pre-existed — is structure. And structure, unlike vigilance, doesn&#39;t get tired at 4 a.m., which is incidentally when several of these landings happened.</p><p>That is the actual demonstration. Not that an AI can write a lot of code — that is old news. That <strong>reduction to modularity, the most meticulous work in software, survives being directed casually when the care lives in contracts, gates, and a substrate that makes carelessness fail loudly instead of mysteriously.</strong></p><p>The greps are in the repo. Run them.</p>',17)])])}const b=i(r,[["render",s]]);export{f as __pageData,b as default};
