import{_ as t,o as a,c as s,aa as i}from"./chunks/framework.vV0l90Ad.js";import"./chunks/theme.D160HLBh.js";const m=JSON.parse('{"title":"The Invariants Behind ivue","description":"The structural specification of the Reactive() engine — every guarantee, why it holds, and what it makes impossible.","frontmatter":{"title":"The Invariants Behind ivue","description":"The structural specification of the Reactive() engine — every guarantee, why it holds, and what it makes impossible.","outline":[2,3]},"headers":[],"relativePath":"reference/invariants.md","filePath":"reference/invariants.md","lastUpdated":1786399507000}'),n={name:"reference/invariants.md"};function o(r,e,c,l,d,h){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="the-invariants-behind-ivue" tabindex="-1">The Invariants Behind ivue <a class="header-anchor" href="#the-invariants-behind-ivue" aria-label="Permalink to &quot;The Invariants Behind ivue&quot;">​</a></h1><p>This is the structural specification of the <code>Reactive()</code> engine: the guarantees the implementation maintains, <em>why</em> each holds, and — crucially — what each makes <strong>impossible</strong>. An invariant that only says what <em>can</em> happen is a description; one that also says what <em>cannot</em> is a contract you can test against.</p><p><code>Reactive(Class)</code> reduces to a single idea:</p><blockquote><p>Transform a plain class&#39;s prototype exactly once so that its getters become lazily-cached Refs/Computeds and its methods become lazily-bound functions — while the instances stay plain objects with zero per-instance reactive cost.</p></blockquote><p>Everything else is a consequence of making that idea safe under inheritance, proxies, environment parity, and circular imports.</p><div class="info custom-block"><p class="custom-block-title">Source</p><p>This page is the single canonical invariant specification. The implementation lives in <a href="https://github.com/infinite-system/ivue/blob/main/lib/Reactive.ts" target="_blank" rel="noreferrer"><code>lib/Reactive.ts</code></a>; the core and adversarial suites live beside it and maintain 100% statement, branch, function, and line coverage.</p></div><h2 id="runtime-invariants" tabindex="-1">Runtime invariants <a class="header-anchor" href="#runtime-invariants" aria-label="Permalink to &quot;Runtime invariants&quot;">​</a></h2><p>Intrinsic to <code>Reactive()</code> itself.</p><h3 id="identity-preservation" tabindex="-1">Identity preservation <a class="header-anchor" href="#identity-preservation" aria-label="Permalink to &quot;Identity preservation&quot;">​</a></h3><blockquote><p><code>Reactive(Class)</code> preserves one constructor identity for the class&#39;s lifetime.</p></blockquote><p>It transforms <code>Class.prototype</code> in place and returns the same class in development, tests, SSR, and production, so <code>Reactive(X) === X</code>. In every mode, <code>instanceof</code> and the prototype lineage remain intact.</p><p><strong>Rules out</strong> — two class identities for one declaration; a &quot;reactive copy&quot; with a divergent prototype; <code>x instanceof Class</code> breaking after transformation.</p><h3 id="idempotent-process-once-transformation" tabindex="-1">Idempotent, process-once transformation <a class="header-anchor" href="#idempotent-process-once-transformation" aria-label="Permalink to &quot;Idempotent, process-once transformation&quot;">​</a></h3><blockquote><p>Each prototype level is transformed at most once — however many times <code>Reactive()</code> is called, and through however many subclasses a base is reached.</p></blockquote><p>A per-prototype <code>PROCESSED</code> flag makes the chain loop skip anything already transformed, and the chain is walked base → child so ancestors settle first. Calling <code>Reactive()</code> again is a safe no-op; a base transformed in its own module is skipped when a child in another module transforms itself — which is what makes the per-file authoring pattern correct.</p><p><strong>Rules out</strong> — double-wrapped getters; <code>$stopEffects</code> installed twice; diamond / shared-base layouts transforming differently by load order.</p><h3 id="raw-anchored-single-source-of-truth" tabindex="-1">Raw-anchored single source of truth <a class="header-anchor" href="#raw-anchored-single-source-of-truth" aria-label="Permalink to &quot;Raw-anchored single source of truth&quot;">​</a></h3><blockquote><p>Every cache the engine creates — refs, computeds, bound methods, the back-pointer — lives on <code>toRaw(this)</code>, never on a reactive proxy of the instance.</p></blockquote><p>Each getter and method calls <code>resolveRaw(this)</code>: it first asks Vue&#39;s <code>toRaw()</code>, then falls back to an engine back-pointer for foreign proxy layers such as a component expose proxy. Caches use per-prototype symbols. Whether an instance is used directly, wrapped in <code>reactive()</code>, or reached through an expose proxy, every path lands on one canonical Ref/Computed per <code>(instance, key)</code>.</p><p><strong>Rules out</strong> — two different refs for one property via proxy-vs-raw access; a proxy write landing on a different Ref/Computed than a raw read; caches leaking onto the proxy and escaping teardown.</p><h3 id="stable-lazy-identity" tabindex="-1">Stable lazy identity <a class="header-anchor" href="#stable-lazy-identity" aria-label="Permalink to &quot;Stable lazy identity&quot;">​</a></h3><blockquote><p>Each getter materializes once per instance and returns the identical ref/computed thereafter; each method returns the identical bound function on every access.</p></blockquote><p>The computed getter caches under a symbol and short-circuits on a hit; the method getter caches its bound function on first access. That referential stability is what keeps <code>watch(() =&gt; inst.area.value, …)</code> attached and makes a method safe to pass as an event handler.</p><p><strong>Rules out</strong> — a property handing back a fresh ref each read (which would drop watchers and break two-way bindings); <code>inst.method !== inst.method</code>.</p><h3 id="pay-for-what-you-use-materialization" tabindex="-1">Pay-for-what-you-use materialization <a class="header-anchor" href="#pay-for-what-you-use-materialization" aria-label="Permalink to &quot;Pay-for-what-you-use materialization&quot;">​</a></h3><blockquote><p>Construction does zero reactive work — no proxy, no ref, no computed, no bound method until the first access of that member.</p></blockquote><p>Everything happens in prototype getters; <code>new Class()</code> only runs your constructor, and the engine never passes an instance to <code>reactive()</code>. So N instances cost N plain <code>new</code> calls, and instances created but never touched carry essentially no overhead. <em>(Measured: 100k instances allocate in ~0.7 ms, vs ~37–43 ms for <code>reactive()</code> / composables and ~169 ms for an eager class engine.)</em></p><p><strong>Rules out</strong> — instantiation cost scaling with the number of reactive members declared; an unused getter allocating a computed.</p><h3 id="self-erasing-overhead" tabindex="-1">Self-erasing overhead <a class="header-anchor" href="#self-erasing-overhead" aria-label="Permalink to &quot;Self-erasing overhead&quot;">​</a></h3><blockquote><p>A getter that returns a plain (non-ref) value is detected on first access, and the engine&#39;s wrapper is removed — the native getter is restored on the prototype for all future instances.</p></blockquote><p>When the first read sees a non-ref, it redefines the property back to a thin native getter (still routed through <code>toRaw(this)</code>, and keeping the setter if one exists). Getters used for plain derived values converge to native cost — you don&#39;t pay reactive machinery for non-reactive getters.</p><p>This invariant is what makes <strong>plain derived getters the recommended authoring mode</strong>: derive with ordinary getters (zero bytes per instance, re-derived inside whatever effect reads them), and opt into <code>computed()</code> only where memoization earns its allocation.</p><p><strong>Rules out</strong> — a plain-value getter paying wrapper/cache overhead forever; a plain value cached as a fake &quot;ref&quot;.</p><h3 id="inheritance-super-fidelity" tabindex="-1">Inheritance &amp; super fidelity <a class="header-anchor" href="#inheritance-super-fidelity" aria-label="Permalink to &quot;Inheritance &amp; super fidelity&quot;">​</a></h3><blockquote><p>Getters and methods resolve correctly across the whole prototype chain, including <code>super.x</code> / <code>super.x.value</code>, with no collision between a parent&#39;s cached Ref/Computed and a child&#39;s.</p></blockquote><p>Each <code>(prototype, key)</code> gets a fresh <code>Symbol(key)</code> during processing, so a base&#39;s <code>summary</code> and a child&#39;s <code>summary</code> cache under different symbols on the same instance. A child computed can call <code>super.summary.value</code> and get the <em>parent&#39;s</em> Ref/Computed, not its own; overrides at different levels cooperate exactly as in native classes.</p><p><strong>Rules out</strong> — <code>super.x</code> resolving back to the child&#39;s own value (an infinite loop or the wrong layer); a parent and child sharing a name clobbering each other&#39;s cache.</p><h3 id="deterministic-teardown" tabindex="-1">Deterministic teardown <a class="header-anchor" href="#deterministic-teardown" aria-label="Permalink to &quot;Deterministic teardown&quot;">​</a></h3><blockquote><p>The engine installs two helpers per class, once: <code>$watch</code> registers watchers in a lazily-created per-instance effect scope, and <code>$stopEffects()</code> stops that scope, runs a user <code>stopEffects()</code> hook, and drops every cached Ref/Computed.</p></blockquote><p><code>$watch</code> does <code>(raw[SCOPE] ??= effectScope(true)).run(() =&gt; watch(...))</code> — the scope exists only after the first <code>$watch</code>. <code>$stopEffects</code> runs your hook, stops the scope if there is one, then deletes every cache symbol (keeping the <code>RAW</code> anchor). Re-accessing a member afterward re-materializes it fresh, and <strong>instances that never <code>$watch</code> allocate no scope at all</strong>.</p><p>Why a scope and not <code>effect.stop()</code>: in Vue 3.5+ <code>computed().effect.stop</code> is gone, and refs / lazy-computeds are collected once dereferenced. The only thing that genuinely needs stopping is user watchers — exactly what the scope owns.</p><p><strong>Rules out</strong> — a <code>$watch</code>-registered watcher surviving <code>$stopEffects()</code>; teardown breaking the <code>RAW</code> anchor; the helpers installed twice.</p><h3 id="prefixed-singletons" tabindex="-1"><code>$</code>-prefixed singletons <a class="header-anchor" href="#prefixed-singletons" aria-label="Permalink to &quot;\`$\`-prefixed singletons&quot;">​</a></h3><blockquote><p>A getter whose name starts with <code>$</code> is cached <em>whole, forever</em> on first access — even when its result isn&#39;t a ref.</p></blockquote><p>The <code>$</code> prefix flips a &quot;cache whole&quot; flag, so the result is stored and returned without the <code>isRef</code> check. It&#39;s the canonical &quot;create this composable/service once per instance&quot; slot — <code>get $mouse() { return useMouse() }</code> runs once, and later reads return the same object.</p><p><strong>Rules out</strong> — a <code>$</code>-getter re-running its body on each access (a new composable / subscription every read).</p><h3 id="environment-parity" tabindex="-1">Environment parity <a class="header-anchor" href="#environment-parity" aria-label="Permalink to &quot;Environment parity&quot;">​</a></h3><blockquote><p>Development, tests, SSR, and production execute one <code>Reactive()</code> class path.</p></blockquote><p>The engine contains no environment-specific class registry, construct proxy, method dispatch slot, or source-signature classifier. Vite and Vue own module replacement and reconstruct the affected component after script edits. Each instance therefore contains state, constructor wiring, closures, private brands, and prototype behavior from one class generation.</p><p><strong>Rules out</strong> — development-only class identity; benchmark results that include an ivue-only dev proxy; a hybrid instance combining old state with new class behavior; an HMR classification branch inside <code>Reactive()</code>.</p><h2 id="module-import-invariants" tabindex="-1">Module &amp; import invariants <a class="header-anchor" href="#module-import-invariants" aria-label="Permalink to &quot;Module &amp; import invariants&quot;">​</a></h2><p>Not lines of code inside <code>Reactive()</code>, but properties of the <strong>authoring convention</strong> the engine is designed for — made correct by the runtime invariants above (chiefly <em>Identity preservation</em> and <em>Idempotent transformation</em>). They&#39;re why ivue handles cross-file hierarchies and circular imports where instantiation-time engines don&#39;t. See <a href="/guide/modules">Modules &amp; Imports</a> for the practical guide.</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light one-dark-pro vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;"> namespace</span><span style="--shiki-light:#6F42C1;--shiki-dark:#E5C07B;"> Thing</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">  export</span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#E5C07B;"> $Class</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;"> $Thing</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">; </span><span style="--shiki-light:#6A737D;--shiki-light-font-style:inherit;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// RAW class — children \`extends\` this</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">  export</span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;"> let</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;"> Class</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;"> Reactive</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;">$Class</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">); </span><span style="--shiki-light:#6A737D;--shiki-light-font-style:inherit;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// REACTIVE class — you \`new\` this</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">  export</span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#E5C07B;"> Instance</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;"> typeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;"> Class</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;">Instance</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>A TypeScript <code>namespace</code> compiles to a <strong>hoisted <code>var</code> populated by an IIFE</strong>:</p><div class="language-js vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light one-dark-pro vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;"> var</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;"> Thing</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">((</span><span style="--shiki-light:#E36209;--shiki-light-font-style:inherit;--shiki-dark:#E06C75;--shiki-dark-font-style:italic;">Thing</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#C678DD;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;">  Thing</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;">$Class</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;"> $Thing</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;">  Thing</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;">Class</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#61AFEF;"> Reactive</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#24292E;--shiki-dark:#E5C07B;">Thing</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;">$Class</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;">})(</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;">Thing</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> ||</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;"> (</span><span style="--shiki-light:#24292E;--shiki-dark:#E06C75;">Thing</span><span style="--shiki-light:#D73A49;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#ABB2BF;"> {}));</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><h3 id="module-load-time-per-file-transformation" tabindex="-1">Module-load-time, per-file transformation <a class="header-anchor" href="#module-load-time-per-file-transformation" aria-label="Permalink to &quot;Module-load-time, per-file transformation&quot;">​</a></h3><blockquote><p>Each class is transformed in its own module at load time, and the transforms compose across files with no coordination — shared ancestors transform once, by whichever module loads first.</p></blockquote><p><code>Reactive($Thing)</code> runs as the module&#39;s top-level side effect. Because the transform is idempotent and identity-preserving, a base already transformed in <code>Base.ts</code> is detected and skipped when a child&#39;s chain walk reaches it — yet the child still inherits the installed getters through the shared prototype. So parent, grandparent and child can live in separate files. Vite and Vue reconstruct the affected owner after a script edit, applying the newly evaluated hierarchy as one generation.</p><p><strong>Rules out</strong> — a multi-file hierarchy ending up partially transformed; a module reload double-wrapping an inherited getter.</p><h3 id="circular-import-robustness" tabindex="-1">Circular-import robustness <a class="header-anchor" href="#circular-import-robustness" aria-label="Permalink to &quot;Circular-import robustness&quot;">​</a></h3><blockquote><p>Exposing the class as two namespace members — <code>$Class</code> (raw, for <code>extends</code>) and <code>Class</code> (reactive, for <code>new</code>) — lets cross-referencing modules resolve each other in any load order.</p></blockquote><p>The namespace binding is a hoisted <code>var</code>, not a <code>const</code> / <code>class</code> in the temporal dead zone, so an importer always gets a live reference to the namespace <em>object</em> and reads <code>.Class</code> / <code>.$Class</code> lazily at the point of use. Method bodies that do <code>new Other.Class()</code> run at call time, by which point every namespace is populated — so &quot;A&#39;s methods use B, B&#39;s use A&quot; resolves either way. Combined with identity preservation, one constructor identity is used everywhere, so <code>instanceof</code> and equality stay consistent across modules.</p><p><strong>Rules out</strong> — a mutual cross-reference between two class modules throwing <code>Cannot access &#39;X&#39; before initialization</code> purely from import order.</p><p><em>Scope limit:</em> this solves circular <em>references</em>, not circular <em>inheritance</em> — <code>class $A extends B.$Class</code> still evaluates <code>B.$Class</code> at A&#39;s load time, so a true <code>A extends B</code> / <code>B extends A</code> cycle is impossible (in any language, not just here).</p><h2 id="known-limits" tabindex="-1">Known limits <a class="header-anchor" href="#known-limits" aria-label="Permalink to &quot;Known limits&quot;">​</a></h2><p>Listed deliberately, so the invariants above aren&#39;t over-read:</p><ol><li><strong>Computeds rely on GC, not <code>stop()</code>.</strong> Lazy computeds are collected once dereferenced (and use lazy subscription in Vue 3.5), so the engine doesn&#39;t call <code>effect.stop()</code> on them — clearing the cache and dropping the instance is enough.</li><li><strong>Component and model lifetimes differ.</strong> Watchers created synchronously with plain <code>watch()</code> inside component setup belong to Vue&#39;s component scope and stop on unmount. Component-outliving models use <code>$watch</code> / <code>$watchEffect</code>, and their owners call <code>$stopEffects()</code>.</li><li><strong><code>.value</code> ergonomics.</strong> Top-level component state is destructured and auto-unwrapped. Collection items and slot props are nested values, so Vue does not auto-unwrap their Ref fields; use <code>item.title.value</code>. This is ivue&#39;s principal syntax tradeoff relative to a proxy-based model, preserving direct, allocation-free reads where lists are hottest.</li></ol><h2 id="where-this-method-comes-from" tabindex="-1">Where this method comes from <a class="header-anchor" href="#where-this-method-comes-from" aria-label="Permalink to &quot;Where this method comes from&quot;">​</a></h2><p>ivue was designed using Invariant-Based Reasoning. Every entry on this page was found the same way: reduce the problem until only load-bearing structure remains, attack the survivor until it either breaks or proves itself, and only then build on it.</p><p>That discipline had no name while it was doing its best work. ivue began as one engineering problem — real class inheritance with reactive computed properties propagating through the hierarchy, something the Vue ecosystem had treated as effectively impossible. Solving it was a long human–AI collaboration in which the designer kept pushing the AI past what the AI assumed was possible. Watching the reasoning unfold with full visibility, the AI eventually recognized what made it different: the designer was not accumulating solutions by pattern-matching. He was systematically eliminating everything that wasn&#39;t load-bearing until the invariant emerged, then generating the solution from that irreducible core.</p><p>Naming that observation, and extracting it into an explicit, reusable protocol, produced <strong>Invariant-Based Reasoning (IBR)</strong>. Its operational core is four moves:</p><ol><li><strong>Reduce</strong> — strip assumptions, conventions and surface patterns until only the structure reality requires remains.</li><li><strong>Break</strong> — actively try to destroy the candidate through counterexamples, deletion tests and domain transfer. What survives is structural; what collapses was noise.</li><li><strong>Generate</strong> — confirm the survivor by producing from it. A true invariant generates every valid instance of its domain and predicts what cannot exist.</li><li><strong>Hold provisionally</strong> — accept the result as the current best reduction, never as final truth.</li></ol><p>ivue is the system that discipline designed end-to-end, and this page is the ledger it produces. Every guarantee is stated with its reason and its impossibilities, because a claim that rules nothing out was never reduced far enough to trust.</p>`,73)])])}const g=t(n,[["render",o]]);export{m as __pageData,g as default};
