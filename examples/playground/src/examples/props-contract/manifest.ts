export const manifest = {
  slug: 'props-contract',
  title: 'Props contract & knobs',
  blurb:
    'The contract is a value on the class — a knobs panel, a validator and an inheritance ledger all read off the statics.',
  docsPath: '/examples/props-and-defaults',
} as const;
