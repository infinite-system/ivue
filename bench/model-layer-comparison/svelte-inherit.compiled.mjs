/* svelte-inherit.svelte.js generated by Svelte v5.56.4 */
import * as $ from 'svelte/internal/client';

export class Base {
	#a = $.state(1);

	get a() {
		return $.get(this.#a);
	}

	set a(value) {
		$.set(this.#a, value, true);
	}

	#total = $.derived(() => this.a + 10);

	get total() {
		return $.get(this.#total);
	}

	set total(value) {
		$.set(this.#total, value);
	}
}

export class Sub extends Base {
	#b = $.state(2);

	get b() {
		return $.get(this.#b);
	}

	set b(value) {
		$.set(this.#b, value, true);
	}

	#total = $.derived(() => super.total + this.b + 100);

	get total() {
		return $.get(this.#total);
	}

	set total(value) {
		$.set(this.#total, value);
	}
}