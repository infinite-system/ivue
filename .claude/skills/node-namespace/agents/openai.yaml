interface:
  display_name: "Node Namespace"
  short_description: "Author circular-safe Node capability classes"
  default_prompt: "Use $node-namespace to build this Node.js backend with static capability classes, lazy-bound callbacks, and late dependency getters."
